{
  "name": "netlab",
  "version": "1.0.0",
  "description": "NetLab — Interactive network learning platform (browser-only)",
  "license": "MIT",
  "scripts": {
    "start": "npx live-server .",
    "dev": "npx live-server .",
    "lint": "eslint js --ext .js",
    "build": "node build.js",
    "test": "node ./scripts/run-sanity-tests.js",
    "test:unit": "vitest run",
    "test:unit:coverage": "vitest run --coverage",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@vitest/coverage-v8": "^4.1.9",
    "esbuild": "^0.18.0",
    "eslint": "^8.0.0",
    "vitest": "^4.1.9"
  }
}
